# Tooling

Neo4j is a graph database with a native vector index. pgvector is an alternative to
Neo4j for vector storage. PGVector and Pgvector are the same thing written different
ways.

Ollama serves local embedding and generation models. LlamaIndex uses Ollama for local
inference.
