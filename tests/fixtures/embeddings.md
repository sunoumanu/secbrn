# Embeddings

An embedding maps text to a vector so that similar passages land close together.
nomic-embed-text produces 768-dimensional vectors and runs locally via Ollama.

## Similarity

Cosine similarity scores how close two vectors are; nearest-neighbour search over
embeddings is what powers vector Retrieval.
